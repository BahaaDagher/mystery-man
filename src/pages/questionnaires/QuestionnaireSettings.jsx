import styled from '@emotion/styled';
import React from 'react'
const Settings = styled("div")(({ theme }) => ({

}));

const QuestionnaireSettings = () => {
  return (
    <>
        <Settings>
            <div>QuestionnaireSettings</div>
        </Settings>
    </>
  )
}

export default QuestionnaireSettings